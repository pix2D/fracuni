services:
  app:
    build: .
    ports:
      - "127.0.0.1:4321:4321"
    environment:
      HOST: 0.0.0.0
      PORT: 4321
      FIRERACUNI_DATA_DIR: /data
    volumes:
      - ../fracuni-data-production:/data
    restart: unless-stopped
